Waf executable version: 1.6.3
Waf executable license: BSD

If you want to compile the binary on debug, add --debug when you execute waf configure.

=== How to install ===
If you have not abready installed Waf, replace 'waf' by './waf' in the followed commands:

   $ waf configure build
   # waf install

or directly in root:

   # waf configure build install

If you using Clang/LLVM as compiler, configure with:

   $ CC=clang waf configure